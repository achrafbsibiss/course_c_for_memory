#include <stdio.h>

int main(void){
  // be for declare the variable we should give him his type
  int age = 100;  // int is a type of variable that can store integer numbers
  float height = 1.85; // float is a type of variable that can store floating point numbers
  char first_letter = 'A'; // char can take one character
  char name[] = "achraf"; // char is a type of variable that can store a single character
  // we can also declare a variable without giving it a value
  printf("%d\n", age);
}